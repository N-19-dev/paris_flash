[project]
name = "paris-flash"
version = "0.1.0"
description = "Hyper-fast static event aggregator for Paris"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "lxml>=6.1.1",
    "mistralai>=2.4.9",
    "requests>=2.34.2",
]

[tool.uv]
package = false
